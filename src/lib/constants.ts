/**
 * Constantes compartilhadas (portado de shared/const.ts da landing,
 * preservando os mesmos valores e sem depender do alias @shared).
 */

export const COOKIE_NAME = "app_session_id";
export const ONE_YEAR_MS = 1000 * 60 * 60 * 24 * 365;